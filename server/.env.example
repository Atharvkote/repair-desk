# ============================================
# Server Configuration
# ============================================
PORT=5000
NODE_ENV=development

# ============================================
# Database Configuration
# ============================================
MONGODB_URI=mongodb://localhost:27017/repair-desk

# ============================================
# Redis Configuration (for rate limiting & Socket.IO)
# ============================================
REDIS_URL=redis://localhost:6379
REDIS_HOST=localhost
REDIS_PORT=6379

# ============================================
# JWT Configuration (RS256 with RSA keys)
# ============================================
# IMPORTANT: Keys must be provided as single-line strings with \n instead of newlines
#
# To generate new RSA keys, run:
# 
# # Using OpenSSL (recommended):
# openssl genrsa -out admin_private.key 2048
# openssl rsa -in admin_private.key -pubout -out admin_public.key
#
# Then convert to single-line format:
# cat admin_private.key | jq -sR 'gsub("\n"; "\\n")'
# cat admin_public.key | jq -sR 'gsub("\n"; "\\n")'
#
# Example format (DO NOT USE - for reference only):
# JWT_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\nMIIEpAIBAAKCAQEA2Z3qX2BTLS..." 
# JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\nMIIBIjANBgkqhkiG9w0BAQE..."

JWT_PRIVATE_KEY=
JWT_PUBLIC_KEY=

# ============================================
# Admin Configuration
# ============================================
# Super admin creation flag (use this to create first super admin)
ADMIN_CYPER=your-super-admin-secret-key

# ============================================
# Backward Compatibility (Optional)
# ============================================
# Keep this for backward compatibility with HMAC tokens if needed
# JWT_SECRET=your-secret-key-here
